# geo-lab

GEO(Generative Engine Optimization) 측정 실험실. **AI 답변에서 브랜드가 어떻게 다뤄지는지를 신뢰구간과 함께 실측한다.**

만들어진 계기 = 스퀘어스 채용 과제(2026-08-07 착수, 마감 2026-08-10). 과제 산출물은 `submission/`, 조사는 `research/`, 측정 도구는 코드로 남는다.

---

## 이 저장소의 전제

**GEO 업계 대부분의 숫자는 단일 측정치다.** LLM은 비결정적이라 같은 프롬프트도 매번 다르게 답한다. 그런데 시중 도구는 대개 1회 실행 결과를 점수로 보여준다.

> 관측 1회는 측정이 아니라 동전 던지기에 가깝다.

이 저장소는 그 반대를 한다 — **반복 측정하고, 분포를 보고, 신뢰구간을 붙인다.** 신뢰구간 없는 수치는 여기서 결론으로 쓰지 않는다.

**이 전제는 이 저장소의 발명이 아니다.** [arXiv 2603.08924](https://arxiv.org/abs/2603.08924)(2026-03)이 같은 논지를 학술적으로 정식화했고, 그 논문이 **B2B와 비영어권을 후속 과제로 남겼다.** 여기서 하는 일은 그 프레임을 그 자리에 적용하는 것이다 — 문제를 처음 지적하는 것이 아니다. 근거는 `research/01` §4·§10.3.

---

## 폴더

```
geo-lab/
├── CLAUDE.md        이 파일. 규칙 정본 (자동 로드 위치라 루트 고정)
├── README.md        외부 독자의 진입점
├── docs/            이 프로젝트에 대해 확인된 것
│   ├── DECISIONS.md 판단 기록 — 무엇을 버렸고 왜
│   └── measurements/ 실측 원자료 (첫 측정 때 생성)
├── inbox/           미검증 인박스 — 근거로 인용 금지
│   └── research/    조사 덤프
├── research/        검증·판정을 거친 조사 정본 — 인용 가능
├── submission/      과제 원문(불변) + 제출물
├── geolab/          측정 도구
└── tests/           순수 함수 테스트
```

| 어디 | 무엇 | 판별 질문 |
|---|---|---|
| `docs/` | 결정 기록·실측 원자료 | 이 프로젝트에 대해 **확인된** 것인가? |
| `inbox/` | 덤프·중간 조사·판정 전 메모 | 나중에 버려도 되는가? |
| `research/` | 출처 교차검증과 판정을 마친 조사 | 근거로 인용해도 되는가? |
| `submission/` | 과제 원문과 답변 | 제출과 직접 관련되는가? |
| `geolab/` · `tests/` | 측정 코드 | 실행되는가? |

**루트는 기본값이 아니다.** 루트에 두려면 *"없으면 저장소가 성립하지 않는가"*에 답해야 한다. `README.md`·`CLAUDE.md`·빌드 설정만 그 답을 갖는다. 답이 *"우리한테 필요하다"*이면 `docs/`다.

**`docs/`와 `research/`의 경계** — `research/`는 **바깥 세계**를 조사한 것이고(이 프로젝트가 없어도 참), `docs/`는 **이 프로젝트**에 대해 확인된 것이다(우리가 무엇을 결정했나, 우리가 무엇을 쟀나).

**실측 원자료는 `docs/measurements/`에 둔다.** 모델 응답은 재현할 수 없다 — 같은 시각의 답변을 다시 만들 수 없고 모델이 갱신되면 영영 못 만든다. 재현 불가능한 것은 버리지 않는다.

**`submission/00-과제-원문.md`는 불변이다.** 요구사항 해석이 갈리면 거기로 돌아간다.

**흐름은 한 방향이다.**

```
inbox/  ──검증·판정──▶  research/
        ──무용────────▶  삭제
```

성숙한 문서를 `inbox/`로 되돌리는 것은 승격이 아니라 강등이다.

**인박스는 비어 있는 것이 정상이다.** 차 있으면 처리가 밀린 것이고, 처리 결과의 대부분은 삭제다. 버릴 수 있어야 담는 것이 쉬워진다.

> 글로벌 규약은 이 폴더를 `seed/`라 부른다. 여기서는 `inbox/`를 쓴다 — 이름이 곧 용도를 말해야 하고, "씨앗"은 심으면 자란다는 뜻이라 **버리는 것이 정상**이라는 성격과 어긋난다.

### 왜 `best-practices/`가 아니라 `research/`인가

글로벌 규약의 승격 목적지는 `best-practices/`다. 여기서는 다르게 정한다 — **이 저장소는 제출물이고, 폴더 이름을 외부 독자가 읽는다.** `research/`가 무엇이 들었는지 즉시 읽힌다. 글로벌 §5가 프로젝트별 재정의를 허용하되 정의를 요구하므로, 이 절이 그 정의다.

---

## Frontmatter

| 계층 | 요구 |
|---|---|
| `inbox/` | `created` **하나만.** 문턱이 높으면 안 던지게 된다 |
| `research/` | `type` · `status` · `updated` · `verified_at` · `sources` |
| `submission/` | `type` · `updated` |

```yaml
---
type: research | submission | tool-doc
status: draft | active | superseded
updated: YYYY-MM-DD
verified_at: YYYY-MM-DD     # 내용을 실제로 확인한 날
sources: 7                  # 교차검증에 쓴 출처 수
---
```

- 열거값(`type`·`status`)은 위 목록 밖으로 나가지 않는다. 새 값이 필요하면 이 표를 먼저 고친다
- 날짜는 `YYYY-MM-DD` 고정. "최근"·"작년" 같은 상대 표현을 쓰지 않는다
- `README.md`·`CLAUDE.md`는 frontmatter를 달지 않는다 (관습 파일)
- `verified_at`은 **내용을 실제로 확인한 날**이다. 파일을 고친 날(`updated`)과 다르다. 오타 하나를 고쳐도 `updated`는 오늘이 되지만 내용 검증은 그대로다 — stale 판정의 기준은 `verified_at`이다

---

## 근거 표기 규칙

이 저장소의 존재 이유가 정직한 측정이므로, 표기 규칙이 곧 제품 규격이다.

1. **수치는 지어내지 않는다.** 출처 없는 숫자를 쓰지 않는다
2. **상관과 인과를 섞지 않는다.** 상관이면 상관이라고 쓴다
3. **측정 조건을 함께 적는다** — 어느 모델, 몇 회 반복, 언제. 조건 없는 수치는 재현 불가라 근거가 아니다
4. **직접 측정한 것과 인용한 것을 구분한다.** 인용에는 출처와 그 출처의 날짜를 붙인다
5. **한계를 먼저 적는다.** 무엇을 측정하지 못했는지 명시한다

---

## 갱신 트리거

| 무엇이 바뀌면 | 무엇을 함께 본다 |
|---|---|
| `inbox/`에 덤프 추가 | 판정 → `research/` 승격 → **인박스에서 삭제**. 남겨두면 정본이 둘이 된다 |
| `research/` 내용 수정 | 그 문서의 `updated` · 그것을 근거로 쓴 `submission/` 문서 |
| 측정 코드 변경 | 그 코드로 낸 수치가 실린 문서 전부 — 측정 방법이 바뀌면 옛 수치는 비교 불가다 |
| 폴더·스키마 변경 | 이 파일 · `README.md` |

---

## 글쓰기

- 한국어. 전문용어 첫 등장 시 괄호 풀이
- 한자·전각 문자 금지
- 표와 불릿으로 스캔 가능하게. 억지 압축 금지 — 명확함이 목적
- 외부에 나갈 문서에 로컬 경로·내부 용어를 남기지 않는다

---

## 측정 코드 규약

측정 도구는 이 저장소의 주장을 코드로 집행하는 부분이다. 규칙이 곧 규격이다.

- **순수 함수를 분리하고 테스트를 붙인다.** 신뢰구간 계산과 브랜드 탐지는 외부 호출 없이 검증 가능해야 한다
- **실패한 호출은 표본에서 제외한다.** "언급 안 됨"으로 세면 언급률이 아래로 편향된다 — 조용히 틀리는 전형적인 방식
- **오탐 0이 커버리지보다 우선한다.** 측정값이 한 번이라도 헛짚으면 이후 숫자 전체가 못 믿을 것이 된다. 못 잡은 것은 한계로 표시한다
- **엔진을 합산하지 않는다.** 플랫폼마다 인용 행동이 달라 합산 점수는 구성비에 좌우된다
- **샘플링 파라미터를 설정하지 않는다.** 재려는 것이 변동 그 자체이고, 현행 Claude 모델은 `temperature` 계열을 아예 거부한다(400)
- **원 응답을 전부 저장한다.** 인용 가능한 증거이자 재분석의 재료다

```bash
python -m pytest tests/ -q     # 순수 함수 검증
```

## 자격증명

- 값은 `저장소 바깥의 자격증명 파일` 과 사용자 환경변수에 있다. **저장소에는 어떤 형태로도 넣지 않는다**
- `.gitignore` 가 `.env*` 를 막고 있다. 우회하지 않는다
- 문서·커밋·리포트에 키 값이나 그 일부를 적지 않는다
