# 측정 대상 모델의 공식 SDK
anthropic>=0.97.0
openai>=2.35.0

# 테스트
pytest>=8.0

# 2차 분석 전용 — geolab/modeling.py (회귀) 와 geolab/embedding.py (군집화) 만 쓴다.
# 설치하지 않아도 측정과 집계는 그대로 돌아간다.
numpy>=2.0
scikit-learn>=1.5
statsmodels>=0.14

# 의존성 경계에 관하여
#
# 측정 코어(stats.py · brands.py · measure.py · prompts.py · engines.py)와
# 2차 분석(analysis.py)은 표준 라이브러리만 쓴다. 리뷰어가 아무것도 설치하지 않고
# 신뢰구간 계산과 브랜드 탐지를 읽고 돌려볼 수 있어야 하기 때문이다 —
# 이 저장소의 주장이 걸려 있는 코드가 바로 그 부분이다.
#
# 위의 수치 라이브러리는 군집 보정 표준오차와 군집화에만 필요하고, 그 두 모듈은
# 지연 import 를 쓴다. 없으면 해당 절만 빠지고 나머지 측정은 영향받지 않는다.
